/**
 * utils/index.js
 * Barrel export — import from 'utils' instead of individual files.
 *
 * Usage:
 *   import { formatINR, timeAgo, isValidPhone } from '../utils';
 */

export * from './formatters';
export * from './validators';
